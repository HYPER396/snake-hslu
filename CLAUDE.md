# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A Snake game implemented in Python. Entry point is `main.py`.

## Running

```bash
python main.py
```

## Stack

- Python (no framework decided yet — the project is in early stages with a single empty `main.py`)
- Likely candidate libraries: `pygame` for graphics/input, or `curses` for terminal-based rendering
